/*
package : import Carousel from 'react-native-reanimated-carousel';

jsx code : 

 <Carousel
          loop
          width={width}
          height={width / 2}
          // autoPlay={true}
          data={sliderData?.sliders?.edges}
          scrollAnimationDuration={1000}
          renderItem={({index, item}) => (
            <View>
              <Image
                style={{
                  width: width<355 ? width*0.95 : width*0.95,
                  height: width / 2,

                  borderRadius: 4,
                }}
                resizeMode="cover"
                source={{uri: item?.node?.image}}
              />
            </View>
          )}
        />

*/